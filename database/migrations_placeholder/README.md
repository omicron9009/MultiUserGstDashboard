# Migration Placeholder

This directory is reserved for database migration tooling (for example, Alembic).

Recommended next step:

1. Initialize Alembic in this project.
2. Point `target_metadata` to `database.core.base.Base.metadata`.
3. Generate the first migration from the models under `database/`.

